# Imagem para operações de changeset
FROM docker.io/oven/bun:1.3.8

RUN apt-get update && apt-get install -y --no-install-recommends \
    git \
    && apt-get clean \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
