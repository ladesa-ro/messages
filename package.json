{
  "private": true,
  "workspaces": ["apis/*"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "version": "bunx changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}
